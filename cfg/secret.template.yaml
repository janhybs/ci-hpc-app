# name of the project
hello-world:
  
  # connection to the database
  database:
    type:               mongodb
    host:               server.example.com:27017
    authSource:         admin
    authMechanism:      SCRAM-SHA-1

    username:           username              # username and password are in a raw form
    password:           password              # make sure this file is properly protected
                                              # i.e. chmod 0400
    
  # database and collection name
  artifacts:
    db_name:              fln-2
    col_timers_name:      timers
    col_index_info_name:  files
    col_scheduler_name:   hist
